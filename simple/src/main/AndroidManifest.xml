<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:name="com.itoys.android.simple.SimpleApp"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:label="${app_name}"
        android:supportsRtl="true"
        android:theme="@style/Theme.IToysAndroid"
        tools:replace="android:allowBackup,android:label">

        <activity
            android:name="com.itoys.android.simple.splash.SplashActivity"
            android:exported="true"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.IToysAndroid.Starting">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="com.itoys.android.simple.list.SimpleListActivity"
            android:screenOrientation="portrait" />
    </application>
</manifest>